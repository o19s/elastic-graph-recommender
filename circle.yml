machine:
  node:
    version: 0.11.8
dependencies:    
  post:
    - ./app/depends.sh
test:
  post:
    - ./app/test.sh
deployment:
  prod:
    branch: master
    commands:
      - aws s3 sync app s3://recommendor.dev.o19s.com/ --delete
